{
  "name": "baseline-ai-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cf dev",
    "deploy": "node -e \"console.error('Use the cf Worker deployment workflow in docs/DEPLOYMENT.md; do not deploy with npm run deploy.'); process.exit(1)\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "@cloudflare/wrangler-bundler": "^0.1.3",
    "typescript": "^5.9.3",
    "wrangler": "^4.76.0"
  }
}
